export function LabelChip({ label }) {
  return (
    <span className="inline-flex items-center rounded-full border border-gray-300 bg-gray-200 px-3 py-1 text-sm text-gray-800">
      {label}
    </span>
  );
}
